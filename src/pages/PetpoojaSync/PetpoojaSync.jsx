import React from 'react';
export default function PetpoojaSync() {
  return (
    <div style={{ padding:'32px 28px', color:'var(--inv-text)', fontFamily:'var(--font-main)' }}>
      <p style={{ fontFamily:'var(--font-label)', fontSize:'10px', letterSpacing:'3px', color:'var(--inv-primary)', marginBottom:'8px' }}>COMING NEXT</p>
      <h1 style={{ fontSize:'28px', fontWeight:'800', marginBottom:'8px' }}>PetpoojaSync</h1>
      <p style={{ color:'var(--inv-text-muted)', fontSize:'14px' }}>This module is being built.</p>
    </div>
  );
}
